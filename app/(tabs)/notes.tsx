import Content from "@/components/Content";
import { Text } from "react-native";

const NotesScreen = () => {
	return (
		<Content>
			<Text>Notes</Text>
		</Content>
	);
};

export default NotesScreen;
